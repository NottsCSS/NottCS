<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NottCS.Views.ForgotPasswordPage"
             Title="{Binding Title}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:NottCS.ViewModels;assembly=NottCS"
             xmlns:converters="clr-namespace:NottCS.Converters;assembly=NottCS"
             d:DataContext="{d:DesignInstance viewModels:ForgotPasswordViewModel}">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center"
                     WidthRequest="500"
                     VerticalOptions="Center">
            <Label Text="PASSWORD RECOVERY" FontSize="Large" Margin="10"/>
            <Label Text="Did you forgot your password?"
                   Margin="5,0"/>
            <Entry x:Name="OWAEntry"
                   Text="{Binding ValidatableObject.Value}"
                   Placeholder="OWA Account"
                   Margin="5,0"/>
            <Button Text = "Reset Password"
                    FontSize="Large"
                    HorizontalOptions="FillAndExpand"
                    Command="{Binding ForgotPasswordCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>