<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:customControlCell="clr-namespace:NottCS.CustomControls.ViewCells;assembly=NottCS"
             xmlns:system="clr-namespace:System;assembly=netstandard"
             x:Class="NottCS.CustomControls.IndexedEventTimeSlotPicker"
             x:Name="EventTimeSlotList">
    <ContentView.Content>
        <ListView ItemsSource="{Binding Source={x:Reference EventTimeSlotList}, Path=EventTimeList}"
                  x:Name="ListViewObject"
                  SeparatorVisibility="Default"
                  RowHeight="{x:OnPlatform iOS=80,Android=150,UWP=130}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <customControlCell:EventTimeSlotPickerCell DeleteCommand="{Binding Source={x:Reference ListViewObject}, Path=BindingContext.DeleteCommand}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Footer>
                <Button Command="{Binding Source={x:Reference ListViewObject}, Path=BindingContext.AddCommand}" 
                        Text="Add" />
            </ListView.Footer>
        </ListView>
  </ContentView.Content>
</ContentView>